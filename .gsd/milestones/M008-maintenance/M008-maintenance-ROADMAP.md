# M008-maintenance: Test Coverage and Maintenance

**Vision:** As there are no active tasks, we will address some missing test coverage and fix minor issues.

## Slices

- [x] **S01: Add tests for API routing and Dashboard edge cases** `risk:low` `depends:[]`
  > Improve coverage on `src/safenergy/api/routes.py` and `src/safenergy/api/dashboard.py`.
